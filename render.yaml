services:
  - type: web
    name: propertysales
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
      # Optional: mount or point to a custom services.json file
      # - key: ARCGIS_SERVICES_FILE
      #   value: /data/services.json
